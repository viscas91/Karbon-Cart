export enum AbilityAction {
    Manage = "manage",
    Create = "create",
    Read = "read",
    ReadAll = "read-all",
    Update = "update",
    Delete = "delete",
}

export enum AbilitySubject {
    All = 'all',
    Products = 'products',
    Users = 'users',
}